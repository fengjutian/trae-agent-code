# Hello World Python Script

This is a simple "Hello, World!" Python script created as part of a task.

## Files Created

1. `hello_world.py` - The main Python script that prints "Hello, World!"
2. `test_hello.py` - Python test script to verify the hello world script works
3. `test_hello.bat` - Windows batch file to test the script
4. `README_HELLO.md` - This documentation file

## Usage

### Running the script directly:
```bash
python hello_world.py
```

### Running the Python test:
```bash
python test_hello.py
```

### Running the Windows batch test:
```bash
test_hello.bat
```

## Script Details

The `hello_world.py` script contains:
- A shebang line for Unix-like systems
- A docstring describing the script
- A `main()` function that prints the message
- The standard `if __name__ == "__main__":` guard

## Expected Output

When you run `hello_world.py`, you should see:
```
Hello, World!
```